# VITE_RELAY_URL: WebSocket URL of the production relay server.
# Used by the client when not running on localhost / a LAN IP (see getRelayUrl in src/App.tsx).
# Defaults to wss://ebonstar.onrender.com if unset.
VITE_RELAY_URL="wss://your-relay.example.com"
